
# React CRUD App 


## Build Setup

```bash
# install project
$ git clone 

# open app directory
$ cd react-crud-app

# install dependencies
$ npm i 

# serve with hot reload at localhost
$ npm run dev

# build for production
$ npm build
```
